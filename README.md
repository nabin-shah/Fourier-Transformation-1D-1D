# Fourier-Transformation-1D-2D


### Requirements to run below codes:

     1. Run:  !pip install opencv-python
     2. Run:  !pip install matplotlib
     3. Run:  !pip install imageio
     
     "!" helps to run install within Jupyter Notebook 
